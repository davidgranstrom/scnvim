#!/bin/sh

pipe_loc=/tmp/sc.nvim-pipe
pipe_loc_pid=/tmp/sc.nvim-pipe-pid

if [[ -f "$pipe_loc_pid" ]]; then
  kill $(cat "$pipe_loc_pid")
  rm "$pipe_loc_pid"
fi

if [[ -p "$pipe_loc" ]]; then
  rm "$pipe_loc"
fi
